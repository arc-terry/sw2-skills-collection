# Reference

## Frontmatter tips

- `name` should match the folder name exactly.
- `description` should explain both capability and trigger context.

## Authoring tips

- Keep the main `SKILL.md` concise.
- Move long examples or technical details into `references/`.
- Keep scripts focused and fail with clear errors.
